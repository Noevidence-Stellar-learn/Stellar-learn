/**
 * @stellar-learn/ui — shared React component library.
 *
 * App-specific game UI currently lives in apps/web/src/components/ui. As
 * components stabilise and need to be shared across apps, promote them here.
 */
export {}
